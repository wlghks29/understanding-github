#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS

int main(void){
    
    int n = 0;
    scanf("%d", &n);
    
    if((n % 4 == 0 && n % 100 != 0) || n % 400 == 0){
        printf("%d", 1);
    }else{
        printf("%d", 0);
    }
    
    return 0;
}
